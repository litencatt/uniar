{{ define "members/index.go.tmpl" }}
<html>
<head>
    <title>{{ .title }}</title>
    <style type="text/css" media="screen">
        @import url("/assets/css/regist.css");
    </style>
</head>
<body>
  <a href="/scenes">シーン一覧</a>
  <a href="/regist/1">シーン登録(櫻坂46)</a>
  <a href="/regist/2">シーン登録(日向坂46)</a>
  <form action="/members" method="POST">
    <input type="submit" />    
    <div>
      <table border="1">
        <thead>
          <tr>
            <th>member</th>
            <th>絆Lv</th>
            <th>ディスコグラフィー</th>
          </tr>
        </thead>
        <tbody>
          {{range .members}}
          <tr>
            <td>{{ .Name }}</td>
            <td><input type="text" name="bonds_{{ .MemberID }}" value="{{ .BondLevel }}"></td>    
            <td><input type="text" name="disco_{{ .MemberID }}" value="{{ .Discography }}"></td>    
          </tr>
          {{end}}
        </tbody>
      </table>
    </div>
  </form>
</body>
</html>
{{ end }}
