{{ define "regist/index.go.tmpl" }}
<html>
<head>
    <title>{{ .title }}</title>
    <style type="text/css" media="screen">
        @import url("/assets/css/regist.css");
    </style>
</head>
<body>
  <form action="/regist" method="POST">
    <input type="submit" />    
    <div>
      <table border="1">
        <thead>
          <tr>
            <th>photograph\member</th>
            {{range .s_member}}
            <th>{{ .Name }}</th>
            {{end}}        
          </tr>
        </thead>
        <tbody>
        {{range $pi, $pv := .s_photograph}}
          <tr>
            <td>{{ .Name }}</td>
            {{range $mi, $mv := $.s_member}}
              <td><input type="checkbox" id="{{.Name}}{{$mi}}" name="member_{{$mv.ID}}[]" value="{{$pv.ID}}"></td>    
            {{end}}
          </tr>
        {{end}}
        </tbody>
      </table>
    </div>
  </form>
</body>
</html>
{{ end }}
