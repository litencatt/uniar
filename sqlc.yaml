version: 1
packages:
  - engine: "mysql"
    path: "repository"
    name: "repository"
    schema: "sql/schema.sql"
    queries: "sql/queries/"
    emit_interface: true
    emit_methods_with_db_argument: true
